<div class="text-center">
  <h2>{{name}}</h2>
  <p>{{{get_emoji}}}</p>
  <p>{{description}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <p>
    <span for="img" aria-label="money">💲</span>
    <span class="calorie-count">{{format_count recipe_calories}}</span>
    needed
  </p>
  <p>
    <button class="btn btn-primary">Create Recipe Article!</button>
  </p>
</div>